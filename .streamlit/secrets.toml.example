# Copy this file to `.streamlit/secrets.toml` and fill in your token.
# A Hugging Face token is recommended: the Unlimited-OCR model runs on the
# hosted Space's ZeroGPU, which rate-limits anonymous callers.
# Create one at https://huggingface.co/settings/tokens
HF_TOKEN = "hf_..."
